<%
import_css(theme.static_prefix.fancybox, 'fancybox.css')

import_script(`
<script>
  Fluid.utils.createScript('${url_join(theme.static_prefix.fancybox, 'fancybox.umd.js')}', function() {
    Fluid.plugins.fancyBox();
  });
</script>
`)
%>
